export type CheckoutUser = {
  firstName: string;
  lastName: string;
  postalCode: string;
};

export const CHECKOUT_USER: CheckoutUser = {
  firstName: 'Test',
  lastName: 'User',
  postalCode: 'SW1A 1AA',
};
